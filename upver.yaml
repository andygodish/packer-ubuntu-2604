version:
  file: version.txt
  pattern: '(?s)(.*)'
  group: 1

upstream:
  file: ubuntu.pkr.hcl
  pattern: 'variable "ubuntu_version" \{[^}]*default\s+=\s+"([^"\n]+)"'
  group: 1

changelog:
  file: CHANGELOG.md
